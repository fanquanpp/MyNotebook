# Bubble Sort 思路解析

## 1. 思路分析
冒泡排序（Bubble Sort）是一种简单的排序算法。它重复地走访过要排序的数列，一次比较两个元素，如果它们的顺序错误就把它们交换过来。
- **解法对比**:
    - 冒泡排序: O(n^2), 稳定, 原地。
    - 快速排序: O(n log n), 不稳定, 递归。
- **选择决策表**:
| 场景 | 推荐解法 |
| :--- | :--- |
| 数据量极小 | 冒泡排序 |
| 数据量大 | 快速排序 |
| 需要稳定排序 | 冒泡排序 / 归并排序 |

## 2. 实现步骤
1. **初始化**: 外层循环控制总的遍历次数。
2. **核心循环**: 内层循环 [bubble_sort.c:L10-L17](file:///c:/Apan/Projects/Notebook/C/algorithms/basic/bubble_sort.c#L10-L17) 比较相邻元素并交换。
3. **后处理**: 完成排序后，在 `main` 函数中打印结果。

## 3. 复杂度分析
- **时间复杂度**:
    - 最坏: O(n^2) - 数组完全逆序。
    - 平均: O(n^2) - 随机分布。
- **空间复杂度**: O(1) - 原地排序。

## 4. 易错点提示
- **行号偏移**: 内层循环的边界应为 `n - i - 1` 而不是 `n - 1`。
- **数据溢出**: 交换时使用 `int temp` 存储。
- **性能优化**: 若某一轮无交换，可提前跳出。

```mermaid
graph TD
    A[Start] --> B{i < n-1?}
    B -- Yes --> C{j < n-i-1?}
    C -- Yes --> D{arr[j] > arr[j+1]?}
    D -- Yes --> E[Swap arr[j], arr[j+1]]
    D -- No --> F[j++]
    E --> F
    F --> C
    C -- No --> G[i++]
    G --> B
    B -- No --> H[End]
```
