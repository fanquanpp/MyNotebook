# JavaScript 原型与继承 (Prototypes and Inheritance)

> @Author: Anonymous
> @Category: JS Basics
> @Description: 解释原型、原型链、构造函数与 new；梳理常见继承模式与 ES6 class 的本质，并给出工程实践与安全注意事项。 | Prototypes, prototype chain, constructors, inheritance patterns, and practical guidance.

---

## 目录

1. [原型与原型链](#原型与原型链)
2. [构造函数与 `new`](#构造函数与-`new`)
3. [继承的常见实现](#继承的常见实现)
4. [属性查找、遮蔽与删除](#属性查找、遮蔽与删除)
5. [工程实践与性能](#工程实践与性能)
6. [安全注意：原型污染](#安全注意：原型污染)

---

## 1. 原型与原型链 (Prototype & Prototype Chain)

### 1.1 什么是原型

在 JavaScript 中，对象的属性查找并不只发生在对象自身。当访问 `obj.x` 时：

1. 先在 `obj` 自身属性中查找 `x`
2. 找不到则沿着 `[[Prototype]]`（俗称“原型”）指向的对象继续查找
3. 直到 `null` 为止（链尾）

这个沿着 `[[Prototype]]` 向上查找的结构就是原型链。

### 1.2 `__proto__`、`Object.getPrototypeOf` 与 `prototype`

- `Object.getPrototypeOf(obj)`：读取对象的原型（推荐）
- `Object.setPrototypeOf(obj, proto)`：设置对象的原型（不推荐，影响性能）
- `obj.__proto__`：历史遗留访问器（不推荐）
- `Fn.prototype`：函数对象特有属性，用于 `new Fn()` 创建实例时的原型指向

它们的关系可以用一句话记住：

- 实例对象通过 `[[Prototype]]` 链接到构造函数的 `prototype`

```js
function Foo() {}
const x = new Foo()

Object.getPrototypeOf(x) === Foo.prototype
```

## 2. 构造函数与 `new` (Constructor & new)

`new Fn(...args)` 的关键步骤可以理解为：

1. 创建一个新对象 `obj`
2. 设置 `obj.[[Prototype]] = Fn.prototype`
3. 执行 `Fn`，并把 `this` 绑定到 `obj`
4. 若 `Fn` 显式返回对象，则返回该对象；否则返回 `obj`

用伪代码表示：

```js
function myNew(Fn, ...args) {
  const obj = Object.create(Fn.prototype)
  const ret = Fn.apply(obj, args)
  return (ret !== null && (typeof ret === 'object' || typeof ret === 'function')) ? ret : obj
}
```

## 3. 继承的常见实现 (Common Inheritance Patterns)

### 3.1 原型链继承

```js
function Parent() {}
Parent.prototype.say = function () { return 'parent' }

function Child() {}
Child.prototype = new Parent()
Child.prototype.constructor = Child
```

问题：

- `Child.prototype` 上共享 `Parent` 实例状态（若 Parent 构造函数里初始化引用类型，会导致实例间共享）
- 无法向 `Parent` 构造函数传参

### 3.2 借用构造函数继承（构造函数继承）

```js
function Parent(name) { this.name = name }
function Child(name) { Parent.call(this, name) }
```

优点：可传参、每个实例独立状态。缺点：方法无法复用（每次实例化都复制一份）。

### 3.3 组合继承

结合两者优点：在 `Child` 中 `Parent.call(this, ...)` 初始化实例属性，再用原型链复用方法。

```js
function Parent(name) { this.name = name }
Parent.prototype.say = function () { return this.name }

function Child(name) { Parent.call(this, name) }
Child.prototype = Object.create(Parent.prototype)
Child.prototype.constructor = Child
```

这也是 ES5 下最常用、最稳定的写法之一。

### 3.4 ES6 `class`/`extends` 的本质

`class` 只是更清晰的语法糖，底层仍然是原型链：

- 实例方法在 `Child.prototype`
- 静态方法在 `Child` 本身
- `extends` 建立两条链：
  - `Child.__proto__ = Parent`（继承静态方法）
  - `Child.prototype.__proto__ = Parent.prototype`（继承实例方法）

```js
class Parent {
  say() { return 'parent' }
  static version() { return 1 }
}

class Child extends Parent {
  say() { return super.say() + ' child' }
}
```

## 4. 属性查找、遮蔽与删除 (Lookup, Shadowing, Delete)

- 子对象自有属性会遮蔽原型链同名属性
- `delete obj.x` 只能删除自有属性，删不掉原型上的 `x`
- `in` 会沿原型链查找；`Object.hasOwn(obj, key)` 只看自有属性

```js
const base = { x: 1 }
const obj = Object.create(base)
obj.x = 2

('x' in obj) // true
Object.hasOwn(obj, 'x') // true
delete obj.x
Object.hasOwn(obj, 'x') // false
obj.x // 1（来自原型）
```

## 5. 工程实践与性能 (Best Practices & Performance)

- 避免运行时频繁 `Object.setPrototypeOf`：会使对象“退化”，影响 JIT 优化
- 优先用 `class`/`extends` 或 `Object.create` 明确建立原型关系
- 对需要枚举的对象，尽量用 `Object.keys`/`Object.entries`（只枚举自有可枚举属性）
- 对安全敏感输入，避免把外部数据直接合并到对象原型链相关位置

## 6. 安全注意：原型污染 (Prototype Pollution)

当把不可信输入合并到对象时，若允许写入 `__proto__`/`constructor.prototype` 等字段，可能污染全局对象原型，导致权限绕过或逻辑劫持。

实践建议：

- 合并用户输入时做 key 白名单或过滤：`__proto__`、`prototype`、`constructor`
- 对纯字典对象使用 `Object.create(null)`，避免原型链

```js
const dict = Object.create(null)
dict['__proto__'] = { polluted: true }
({}).polluted // undefined
```

---
### 更新日志 (Changelog)
- 2026-04-06: 新增「原型与继承」知识点，补充继承模式、性能与安全要点
