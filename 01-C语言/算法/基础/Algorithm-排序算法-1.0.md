<!--
文件名称：01-排序算法-学习笔记
原名称：无
用途描述：介绍常见排序算法（冒泡、选择、插入、希尔、归并、快速、堆排序）的原理、C 语言实现及复杂度分析。
创建/更新：2026-04-05
-->

# 01-排序算法（Sorting Algorithms）

排序算法是计算机科学中最基础的算法类别之一。本笔记对比了多种排序算法的性能和适用场景。

## 1. 核心概念

- **稳定性**：排序后相等元素的相对位置是否保持不变。
- **原地排序**：是否需要额外的内存空间。
- **复杂度对比**：

| 排序算法 | 平均时间复杂度 | 最好时间复杂度 | 最坏时间复杂度 | 空间复杂度 | 稳定性 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **冒泡排序** | $O(n^2)$ | $O(n)$ | $O(n^2)$ | $O(1)$ | 稳定 |
| **选择排序** | $O(n^2)$ | $O(n^2)$ | $O(n^2)$ | $O(1)$ | 不稳定 |
| **插入排序** | $O(n^2)$ | $O(n)$ | $O(n^2)$ | $O(1)$ | 稳定 |
| **希尔排序** | $O(n^{1.3})$ | $O(n)$ | $O(n^2)$ | $O(1)$ | 不稳定 |
| **归并排序** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | $O(n)$ | 稳定 |
| **快速排序** | $O(n \log n)$ | $O(n \log n)$ | $O(n^2)$ | $O(\log n)$ | 不稳定 |
| **堆排序** | $O(n \log n)$ | $O(n \log n)$ | $O(n \log n)$ | $O(1)$ | 不稳定 |

## 2. 算法原理与实现

### 2.1 快速排序 (Quick Sort)
- **原理**：通过一个“基准”（Pivot）将数组分为两部分，递归排序。
- **特点**：实际应用中最常用的排序。

```c
void quickSort(int arr[], int left, int right) {
    if (left >= right) return;
    int pivot = arr[left];
    int i = left, j = right;
    while (i < j) {
        while (i < j && arr[j] >= pivot) j--;
        arr[i] = arr[j];
        while (i < j && arr[i] <= pivot) i++;
        arr[j] = arr[i];
    }
    arr[i] = pivot;
    quickSort(arr, left, i - 1);
    quickSort(arr, i + 1, right);
}
```

### 2.2 归并排序 (Merge Sort)
- **原理**：分治法，将数组分成两半，分别排序后合并。
- **特点**：稳定的 $O(n \log n)$ 算法，但需要额外空间。

## 3. 实战应用

- **冒泡/插入排序**：适用于小规模数据或基本有序的数据。
- **快速排序**：通用排序首选，绝大多数标准库的底层实现。
- **堆排序**：适用于寻找前 K 个最大/最小元素的场景（Top-K）。

## 4. 最佳实践

- **稳定性优先**：选归并排序。
- **空间敏感**：选堆排序或快速排序。
- **数据量极小**：选插入排序（常数项更小）。

## 5. 延伸阅读

- [Visualizing Algorithms](https://visualgo.net/en/sorting) <!-- nofollow -->
- [Hello 算法 - 排序](https://www.hello-algo.com/chapter_sorting/) <!-- nofollow -->

## 6. 更新日志

- **2026-04-05**：初始版本，整理七大经典排序。
